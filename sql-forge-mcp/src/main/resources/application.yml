spring:
  main:
    web-application-type: none
    banner-mode: off
  ai:
    mcp:
      server:
        name: sql-forge-mcp
        version: @project.version@

logging:
  pattern:
    console:
  file:
    name: ./mcp/sql-forge-mcp-server.log